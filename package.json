{
  "name": "senior-take-home-test",
  "private": true,
  "version": "1.0.0",
  "packageManager": "yarn@3.4.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "api": "yarn workspace api start",
    "cli": "yarn workspace cli build && node ./dist/packages/cli/src/index.js",
    "test": "jest"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "typescript": "^5.1.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
